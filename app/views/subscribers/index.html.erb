<h1 class="font-bold text-4xl">Subscribe to the thing.</h1>

<%= turbo_frame_tag "subscribers/new", src: new_subscriber_path %>

<h2 class="font-bold text-3xl">Current Subscribers</h2>
<p>Pretend it's not unhinged to list everyone's phone #'s here. 😅</p>

<%= turbo_frame_tag "subscribers" do %>
  <% if @subscribers.any? %>
    <%= render @subscribers %>
  <% end %>
<% end %>
